spring:
  datasource:
    url: jdbc:h2:~/fiapdb;DB_CLOSE_ON_EXIT=FALSE
    username: fiap
    password: fiap
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

jwt:
  secret: s3cr3t1
  expire: 60